########################################################
# Dockerfile to demonstrate the behaviour of CMD
########################################################
FROM alpine:latest

# Set command for CMD
CMD ["echo", "Dockerfile CMD demo"] 
